@*@model IEnumerable<RailRoad.DataPersistence.Entities.Attendance>
<div>
    <table class="table table-hover shadow">
        <thead>
            <tr>
                <td scope="col">Name</td>
                <td scope="col">License Number</td>
                <td scope="col">Adhaar Number</td>
                <td scope="col">Mobile Number<br>(in Rs)</td>
                <td scope="col">Address</td>
                <td scope="col">Salary<br>(in Rs)</td>
                <td scope="col">Designation</td>
            </tr>
        </thead>
        <tbody>
            @if (Model?.Count() > 0)
                @foreach (var employee in Model)
                {
                    <tr>
                        <td>@employee?.Name</td>
                        <td>@employee?.Designation</td>
                        <td><a asp-action="MarkAttendance" asp-route-license="@employee.License"><input type="button" class="btn-info rounded" value="Details"></a></td>
                    </tr>
                }
        </tbody>
    </table>
</div>*@